#include<stdio.h>
#include<stdlib.h>
#include<math.h>
int main()
{
    long int a[10000];
    int s=0,n;
    scanf("%ld",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(int i=0;i<n;i++)
    {
        s=s+a[i];
    }
    printf("%d",s);
    return 0;
}
