#include<stdio.h>
#include<math.h>
int main()
{
    long long int n,i,s=0,a[10000];
    scanf("%lld",&n);
    for(i=1;i<=n;i++)
    {
        scanf("%lld",&a[i]);
    }
    for(int j=1;j<=n;j++)
    {
        s=s+a[j];
    }
    printf("%lld",s);
    return 0;
}
